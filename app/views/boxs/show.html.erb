<div>
  <%= attachment_image_tag @box, :image %>
  <p>収納棚名：<%= @box.title %></p>
  <p>説明：<%= @box.explanation %></p>
  <p>投稿日時：<%= @box.created_at.strftime('%Y/%m/%d') %></p>
  <% if @box.user == current_user %>
    <%= link_to "削除", box_path(@box), method: :delete %>
  <% end %>
</div>